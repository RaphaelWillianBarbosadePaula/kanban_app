<!DOCTYPE html>
<html>
  <head>
    <title>Kanban App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-900 font-sans min-h-screen flex flex-col">

    <nav class="bg-white border-b border-gray-200 shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center text-xl font-bold text-indigo-600" do %>
              Kanban<span class="text-gray-900">Rails</span>
            <% end %>
          </div>

          <div class="flex items-center space-x-4">
            <% if @current_user.present? %>
              <div class="flex items-center space-x-4">
                <%= link_to "Meu Perfil", user_path(@current_user), 
                    class: "text-gray-500 hover:text-indigo-600 px-3 py-2 text-sm font-medium transition" %>
                
                <%= button_to "Sair", logout_path, method: :delete, 
                    class: "text-red-500 hover:text-red-700 px-3 py-2 text-sm font-medium transition" %>
              </div>
            <% else %>
              <%= link_to "Login", login_path, class: "text-gray-500 hover:text-gray-900 px-3 py-2 text-sm font-medium" %>
              <%= link_to "Criar Conta", signup_path, class: "bg-indigo-600 text-white hover:bg-indigo-700 px-4 py-2 rounded-md text-sm font-medium transition" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
      <% flash.each do |key, value| %>
        <div class="p-4 rounded-md <%= key == 'notice' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
          <%= value %>
        </div>
      <% end %>
    </div>

    <main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">
      <%= yield %>
    </main>

  </body>
</html>